%ul.list-inline.list-unstyled
  - if controller_name != 'sessions'
    %li= link_to "Se connecter", new_session_path(resource_name)


  - if devise_mapping.registerable? && controller_name != 'registrations'
    %li= link_to "Devenir conseiller", new_registration_path(resource_name)


  - if devise_mapping.recoverable? && controller_name != 'passwords'
    %li= link_to "J'ai oublié mon mot de passe", new_password_path(resource_name)
