.row
  .col-md-6.col-md-offset-3
    %h1.text-center Modifiez votre mot de passe.

    %p
      Indiquez votre email, nous vous enverrons les instructions afin que vous puissiez créer un nouveau mot de passe.

    = form_for resource, as: resource_name, url: password_path(resource_name), method: :put do |f|

      - if resource.errors.any?
        .alert.alert-danger
          %h5
            Une ou plusieurs érreurs empêchent l'élement d'être enregistré :
          - resource.errors.full_messages.each do |msg|
            %li
              = msg

      .form-group
        %label Mot de passe
        = f.hidden_field :reset_password_token
        = f.password_field :password, class: "form-control"
      .form-group
        %label Confirmez votre mot de passe
        = f.hidden_field :reset_password_token
        = f.password_field :password, class: "form-control"

      .form-group
        %button.btn.btn-success{type: "submit"}
          Créer mon nouveau mot de passe

      = render "devise/shared/links"