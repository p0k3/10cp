<!DOCTYPE html>
%html
  %head
    %title Administration 10 Conseils pour

    %meta{ content: "fr", http:{equiv: "content-language"}}

    %link{ rel: "canonical", href: canonical_url }
    %link{:href => canonical_url, :hreflang => "fr-fr", :rel => "alternate"}/

    = stylesheet_link_tag "admin/application", media: "screen"
    = csrf_meta_tags
    = favicon_link_tag "favicon.ico"

    %meta{ name: "viewport", content: "width=device-width, initial-scale=1"}

  %body{class: body_class}
    #wrapper
      / Navigation
      %nav.navbar.navbar-inverse.navbar-fixed-top{:role => "navigation"}
        / Brand and toggle get grouped for better mobile display
        .navbar-header
          %button.navbar-toggle{"data-target" => ".navbar-ex1-collapse", "data-toggle" => "collapse", :type => "button"}
            %span.sr-only Toggle navigation
            %span.icon-bar
            %span.icon-bar
            %span.icon-bar
          %a.navbar-brand{:href => "index.html"} 10 Conseils Pour - Administration
        / Top Menu Items
        %ul.nav.navbar-right.top-nav
          %li.dropdown
            %a.dropdown-toggle{"data-toggle" => "dropdown", :href => "#"}
              = fa_icon 'user'
              = current_user.fullname
              %b.caret
            %ul.dropdown-menu
              %li
                = link_to destroy_user_session_path, method: :delete do
                  = fa_icon 'power-off'
                  Se déconnecter

        .collapse.navbar-collapse.navbar-ex1-collapse
          = render 'layouts/admin/sidebar'

      #page-wrapper
        .container-fluid
          = yield

    = javascript_include_tag "admin/application"
    = custom_bootstrap_flash