%h1.page-header
  Les thèmes
  = link_to new_theme_path, class: "btn btn-success" do
    %i.fa.fa-plus
    Créer

%table.table
  %tr
    %th #
    %th Label
    %th
  - @themes.each do |t|
    %tr
      %td= t.id
      %td= t.title
      %td= t.description[0..50] + "..."
      %td
        = link_to edit_theme_path(t), class: "btn btn-primary btn-xs" do
          %i.fa.fa-edit
          Modifier
